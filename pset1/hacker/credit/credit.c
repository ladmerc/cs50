#include <stdio.h>
#include <cs50.h>
#include <string.h>

int main(int argc, string argv[])
{
    
    int num = GetInt();
    
    printf("length is %i", strlen( (string) num));sprintf(str, "%d", aInt);
    
    return 0;
}
